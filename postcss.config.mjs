// postcss.config.mjs
const config = {
  plugins: {
    tailwindcss: {}, // Activează Tailwind CSS
    autoprefixer: {}, // Adaugă prefixe vendor automat
    // Alte plugin-uri PostCSS pot fi adăugate aici dacă e necesar
  },
};

export default config;
